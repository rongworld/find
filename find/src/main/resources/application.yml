spring:
  datasource:
    url: jdbc:mysql://localhost:3306/swzl?useSSL=false
    username: root
    password: wr000000
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  mail:
    host: smtp.exmail.qq.com
    username: 用户名
    password: 密码
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
